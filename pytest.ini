[pytest]
testpaths = services/api/tests contracts/tests
pythonpath = services/api
markers =
    integration: requires TEST_DATABASE_URL pointing to PostgreSQL 16
    live: optional provider tests that require real credentials
